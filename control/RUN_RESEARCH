# Touch this file to start a research run on a GitHub runner.
#
# It exists because the token that writes this repository is not granted
# `actions: write`, so a run cannot be dispatched through the API. A push to
# this one path can start one, and scoping the trigger to a path the job never
# writes is what stops a job that commits every five minutes from restarting
# itself forever.
#
# Change the line below to anything and push.

run: 2026-08-31T15:20Z — first LITUSDC run
